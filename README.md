# 🎬 Cinema System - Образовательный проект

Микросервисная архитектура для системы управления кинотеатром с веб-интерфейсом, разработанная на FastAPI и Docker.

---

## 🎯 Что это?

**Cinema System** - это учебный проект, имитирующий реальную систему бронирования и продажи билетов в кинотеатре. Проект состоит из 4 микросервисов, взаимодействующих через REST API, и веб-приложения для управления.

### Характеристики:
- ✅ **Образовательный режим**: Платежи имеют 2/3 вероятность успеха
- ✅ **Микросервисная архитектура**: 4 независимых сервиса
- ✅ **Docker контейнеризация**: Полная поддержка Docker Compose
- ✅ **Dark Theme**: 3D-очки стиль (чёрный, голубой, красный)
- ✅ **Admin Panel**: Защищённый доступ с паролем `123234`
- ✅ **REST API**: Полная документация через Swagger UI
- ✅ **Windows & Linux**: Работает на Windows 10 и развёртывается на Debian 12

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                        WEB BROWSER (Port 80)                    │
│                     🎬 Cinema System UI                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  Session Service │ │  Ticket Service  │ │ Payment Service  │
│    Port 8000     │ │    Port 8001     │ │    Port 8002     │
│ (FastAPI)        │ │  (FastAPI)       │ │   (FastAPI)      │
│                  │ │                  │ │                  │
│ - Сеансы         │ │ - Билеты         │ │ - Платежи        │
│ - Места          │ │ - Бронирования   │ │ - Оплата         │
│ - Залы           │ │ - Отмены         │ │ - Статусы        │
└──────────────────┘ └──────────────────┘ └──────────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                    ┌───────▼────────┐
                    │   Docker       │
                    │   Network      │
                    │ (cinema-net)   │
                    └────────────────┘
```

---

## 🚀 Быстрый старт

### Windows 10 (Текущая разработка)

```powershell
# 1. Открыть PowerShell и перейти в папку проекта
cd "c:\Users\nikit\Desktop\cinema-system"

# 2. Запустить систему
docker-compose up -d

# 3. Открыть браузер
http://localhost
```

[📖 Подробнее → QUICKSTART_WINDOWS.md](QUICKSTART_WINDOWS.md)

### Debian 12 (Развёртывание)

```bash
# На Windows: передать проект
scp -r c:\Users\nikit\Desktop\cinema-system user@192.168.1.100:/home/user/

# На Debian: установить Docker и запустить
ssh user@192.168.1.100
cd ~/cinema-system
docker-compose up -d

# Открыть браузер
http://192.168.1.100
```

[📖 Подробнее → DEBIAN_12_SETUP.md](DEBIAN_12_SETUP.md)

---

## 📁 Структура проекта

```
cinema-system/
├── session-service/              # Сервис управления сеансами
│   ├── app/
│   │   ├── main.py              # FastAPI приложение
│   │   ├── models.py            # БД модели
│   │   ├── schemas.py           # Pydantic схемы
│   │   └── storage.py           # In-memory хранилище
│   ├── Dockerfile
│   └── requirements.txt
│
├── ticket-service/               # Сервис управления билетами
│   ├── app/
│   │   ├── main.py
│   │   ├── schemas.py
│   │   ├── storage.py
│   │   └── session_client.py    # HTTP клиент к session-service
│   ├── Dockerfile
│   └── requirements.txt
│
├── payment-service/              # Сервис обработки платежей
│   ├── app/
│   │   ├── main.py
│   │   ├── schemas.py
│   │   └── ticket_client.py     # HTTP клиент к ticket-service
│   ├── Dockerfile
│   └── requirements.txt
│
├── notification-service/         # Сервис уведомлений
│   ├── app/
│   │   ├── main.py
│   │   ├── logger.py
│   │   └── schemas.py
│   ├── Dockerfile
│   └── requirements.txt
│
├── web-app/                      # Веб-приложение (Nginx + HTML/CSS/JS)
│   ├── public/
│   │   └── index.html           # Dark theme UI с админ-панелью
│   └── Dockerfile
│
├── docker-compose.yml            # Оркестрация контейнеров
├── README.md                     # Этот файл
├── QUICKSTART_WINDOWS.md         # Быстрый старт для Windows
├── DEBIAN_12_SETUP.md            # Подробная инструкция для Debian
├── DEPLOYMENT_GUIDE.md           # Полное руководство развёртывания
└── POSTMAN_EXAMPLES.md           # Примеры API запросов
```

---

## 🔌 Микросервисы

### 1️⃣ Session Service (Port 8000)
Управление кинотеатром: сеансы, залы, места.

**API Endpoints:**
```
GET  /sessions                    - Получить все сеансы
POST /sessions                    - Создать новый сеанс
GET  /sessions/{id}/seats         - Получить места сеанса
PUT  /sessions/{id}/seats/{code}  - Обновить место
GET  /docs                        - Swagger UI
```

### 2️⃣ Ticket Service (Port 8001)
Управление билетами и бронированиями.

**API Endpoints:**
```
GET  /tickets                     - Получить все билеты
POST /tickets/reserve             - Забронировать билет
POST /tickets/confirm/{id}        - Подтвердить билет
POST /tickets/cancel/{id}         - Отменить билет
GET  /docs                        - Swagger UI
```

### 3️⃣ Payment Service (Port 8002)
Обработка платежей (образовательный режим: 2/3 успеха).

**API Endpoints:**
```
POST /payment/init                - Инициировать платёж
GET  /payment/status/{id}         - Получить статус
GET  /docs                        - Swagger UI
```

### 4️⃣ Notification Service (Port 8003)
Система уведомлений о событиях.

**API Endpoints:**
```
GET  /notifications               - Получить все уведомления
POST /notifications/send          - Отправить уведомление
GET  /docs                        - Swagger UI
```

---

## 🎨 Веб-интерфейс

### Дизайн
- **Стиль**: Dark Theme (3D-очки)
- **Цвета**:
  - Чёрный фон: `#0a0e27`
  - Серый: `#1a1a2e`
  - Голубой (левое стекло): `#00bfff`
  - Красный (правое стекло): `#ff0000`

### Функции для посетителя
- ✅ Выбор сеанса
- ✅ Интерактивная сетка мест
- ✅ Бронирование и оплата билетов
- ✅ История купленных билетов
- ✅ Отмена билетов

### Функции для администратора (пароль: 123234)
- ✅ Создание новых сеансов
- ✅ Просмотр всех сеансов
- ✅ Статистика и аналитика
- ✅ Управление залами

---

## 🧪 Тестирование

### Через веб-интерфейс
```
http://localhost              (Windows)
http://192.168.1.100          (Debian)
```

### Через Postman
```
Session Service:    http://192.168.1.100:8000/docs
Ticket Service:     http://192.168.1.100:8001/docs
Payment Service:    http://192.168.1.100:8002/docs
Notification Service: http://192.168.1.100:8003/docs
```

[📖 Примеры запросов → POSTMAN_EXAMPLES.md](POSTMAN_EXAMPLES.md)

### Через curl (Debian)
```bash
# Получить все сеансы
curl http://localhost:8000/sessions

# Создать сеанс
curl -X POST http://localhost:8000/sessions \
  -H "Content-Type: application/json" \
  -d '{"movie_title":"Интерстеллар","hall_id":1,"start_time":"2024-12-20 19:00"}'
```

---

## 📊 Образовательный режим

### Платежный сервис работает с 2/3 вероятностью успеха

```python
success = random.choice([True, True, False])  # 2 True, 1 False
```

Это сделано для имитации реальных ситуаций отказа платежей.

**Результаты при тестировании 10 платежей:**
- ~6-7 успешных платежей ✅
- ~3-4 отклонённых платежа ❌

---

## 🐳 Docker Compose

### Запустить все сервисы
```powershell
docker-compose up -d
```

### Остановить все сервисы
```powershell
docker-compose down
```

### Посмотреть логи
```powershell
docker-compose logs -f
```

### Состояние сервисов
```powershell
docker-compose ps
```

### Пересобрать образы
```powershell
docker-compose build --no-cache
```

---

## 📦 Технологический стек

| Компонент | Версия | Назначение |
|-----------|--------|-----------|
| **Python** | 3.11 | Язык программирования микросервисов |
| **FastAPI** | 0.104+ | Web-framework для API |
| **Uvicorn** | 0.24+ | ASGI сервер |
| **Docker** | 24.0+ | Контейнеризация |
| **Docker Compose** | 2.0+ | Оркестрация контейнеров |
| **Nginx** | Latest | Веб-сервер для фронтенда |
| **HTML5** | - | Структура страниц |
| **CSS3** | - | Стилизация (Dark Theme) |
| **JavaScript** | ES6+ | Логика взаимодействия |

---

## 🔐 Безопасность

### Администратор
- **Пароль**: `123234` (зашифрован на клиентской стороне)
- **Доступ**: Модальное окно на веб-интерфейсе
- **Функции**: Управление сеансами и просмотр статистики

### CORS
- Все сервисы имеют CORS middleware
- Разрешены запросы с фронтенда

### API
- REST API с документацией Swagger
- Валидация входных данных через Pydantic

---

## ⚠️ Известные ограничения

1. **In-Memory Storage**: Данные хранятся в памяти, теряются при перезагрузке
2. **Нет аутентификации**: Посетитель может смотреть все билеты
3. **Образовательный режим**: Платежи не реальные, только для имитации
4. **Single Hall**: Текущая реализация поддерживает несколько залов, но упрощённо

---

## 📚 Документация

| Файл | Описание |
|------|---------|
| [README.md](README.md) | Этот файл |
| [QUICKSTART_WINDOWS.md](QUICKSTART_WINDOWS.md) | Быстрый старт на Windows |
| [DEBIAN_12_SETUP.md](DEBIAN_12_SETUP.md) | Подробная инструкция Debian 12 |
| [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md) | Полное руководство развёртывания |
| [POSTMAN_EXAMPLES.md](POSTMAN_EXAMPLES.md) | Примеры API запросов |

---

## 🚀 Начало работы

### Минимальный вариант (за 2 минуты)
```powershell
cd "c:\Users\nikit\Desktop\cinema-system"
docker-compose up -d
# Открыть http://localhost
```

### Полный вариант (с деталями)
1. Прочитайте [QUICKSTART_WINDOWS.md](QUICKSTART_WINDOWS.md)
2. Следуйте инструкциям для локального тестирования
3. Затем следуйте [DEBIAN_12_SETUP.md](DEBIAN_12_SETUP.md) для развёртывания

### Тестирование API
1. Откройте Swagger UI: `http://localhost:8000/docs`
2. Или используйте примеры из [POSTMAN_EXAMPLES.md](POSTMAN_EXAMPLES.md)

---

## 💡 Примеры использования

### Создать сеанс (curl)
```bash
curl -X POST http://localhost:8000/sessions \
  -H "Content-Type: application/json" \
  -d '{
    "movie_title": "Интерстеллар",
    "hall_id": 1,
    "start_time": "2024-12-20 19:00"
  }'
```

### Забронировать билет (curl)
```bash
curl -X POST http://localhost:8001/tickets/reserve \
  -H "Content-Type: application/json" \
  -d '{
    "session_id": 1,
    "row": "A",
    "number": 5,
    "price": 250
  }'
```

### Обработать платёж (curl)
```bash
curl -X POST http://localhost:8002/payment/init \
  -H "Content-Type: application/json" \
  -d '{
    "ticket_id": 1,
    "amount": 250
  }'
```

---

## 📞 Поддержка и вопросы

### Если что-то не работает:

1. **Проверить логи**:
   ```powershell
   docker-compose logs -f
   ```

2. **Убедиться, что контейнеры запущены**:
   ```powershell
   docker-compose ps
   ```

3. **Перезагрузить систему**:
   ```powershell
   docker-compose down
   docker-compose up -d
   ```

4. **Проверить доступность портов**:
   ```powershell
   netstat -ano | findstr :80
   netstat -ano | findstr :8000
   ```

---

## 📝 История версий

### v1.0.0 (текущая)
- ✅ 4 микросервиса (Session, Ticket, Payment, Notification)
- ✅ Веб-интерфейс с Dark Theme
- ✅ Админ-панель с защитой паролем
- ✅ Образовательный режим (2/3 успеха платежей)
- ✅ Docker Compose оркестрация
- ✅ Полная документация

---

## 📄 Лицензия

Этот проект является учебным материалом и предоставляется в образовательных целях.

---

## 🎓 Для учебных целей

Этот проект разработан как учебный пример микросервисной архитектуры и может быть использован для:
- Изучения FastAPI и микросервисов
- Практики работы с Docker и Docker Compose
- Понимания REST API и inter-service communication
- Изучения веб-разработки (Frontend + Backend)

---

**Готово к использованию! 🎬**

Начните с [QUICKSTART_WINDOWS.md](QUICKSTART_WINDOWS.md) или [DEBIAN_12_SETUP.md](DEBIAN_12_SETUP.md) в зависимости от вашего окружения.

### Доступ к приложению

- **Web App**: http://localhost
- **Session Service API**: http://localhost:8000/docs
- **Ticket Service API**: http://localhost:8001/docs
- **Payment Service API**: http://localhost:8002/docs
- **Notification Service API**: http://localhost:8003/docs

## 📚 API Endpoints

### Session Service (/sessions)

```
GET /sessions
├─ Получить все сеансы

POST /sessions
├─ Создать новый сеанс
├─ Body: {movie_title, hall_id, start_time}

GET /sessions/{session_id}
├─ Получить информацию о сеансе

GET /sessions/{session_id}/seats
├─ Получить все места в сеансе

PUT /sessions/{session_id}/seats/{row}/{number}
├─ Обновить доступность места
├─ Body: {is_available}
```

### Ticket Service (/tickets)

```
GET /tickets
├─ Получить все билеты

GET /tickets/session/{session_id}
├─ Получить билеты для сеанса

POST /tickets/reserve
├─ Забронировать билет
├─ Body: {session_id, row, number, price}

GET /tickets/{ticket_id}
├─ Получить информацию о билете

POST /tickets/confirm/{ticket_id}
├─ Подтвердить билет (оплачен)

POST /tickets/cancel/{ticket_id}
├─ Отменить билет
```

### Payment Service (/payment)

```
POST /payment/init
├─ Инициировать платёж
├─ Body: {ticket_id, amount}
├─ Response: {ticket_id, status, message}
```

### Notification Service (/notify)

```
POST /notify
├─ Отправить уведомление
├─ Body: {ticket_id, message}

GET /notifications
├─ Получить все уведомления
```

## 🎯 Основные функции

### Для Пользователя
- ✅ Просмотр доступных сеансов
- ✅ Выбор мест (интерактивная сетка)
- ✅ Бронирование билетов
- ✅ Оплата билетов
- ✅ Просмотр своих билетов
- ✅ Отмена билетов

### Для Администратора
- ✅ Добавление новых сеансов
- ✅ Управление фильмами и залами
- ✅ Просмотр всех сеансов
- ✅ Статистика продаж
- ✅ Отслеживание доходов

## 🔄 Процесс покупки билета

1. **Пользователь выбирает сеанс**
   - Session Service отправляет список доступных мест

2. **Выбираются места**
   - Ticket Service проверяет доступность в Session Service
   - Создаются забронированные билеты

3. **Инициирует платёж**
   - Payment Service обрабатывает платёж
   - При успехе билет переходит в статус SOLD

4. **Получает уведомление**
   - Notification Service отправляет подтверждение
   - Система обновляет доступность мест

## 📊 Модели данных

### Session
```json
{
  "id": 1,
  "movie_title": "Интерстеллар",
  "hall_id": 1,
  "start_time": "2026-01-12 18:00"
}
```

### Ticket
```json
{
  "id": 1,
  "session_id": 1,
  "row": "A",
  "number": 5,
  "status": "SOLD",
  "price": 250.0
}
```

### TicketStatus
- `RESERVED` - Забронирован
- `SOLD` - Оплачен
- `CANCELLED` - Отменён

## 🐛 Исправленные ошибки

1. **Синхронизация мест** ✅
   - Добавлена API для обновления доступности мест в Session Service
   - Ticket Service теперь обновляет места при бронировании/отмене

2. **Управление статусами** ✅
   - Полный жизненный цикл билета (RESERVED → SOLD → CANCELLED)
   - Корректные переходы между статусами

3. **Взаимодействие сервисов** ✅
   - Исправлены URL для inter-service коммуникации
   - Добавлена сетевая настройка в docker-compose

4. **CORS для веб-интерфейса** ✅
   - Все микросервисы имеют CORSMiddleware
   - Фронтенд может делать запросы со всех портов

## 🛠️ Разработка

### Структура проекта

```
cinema-system/
├── session-service/
│   ├── app/
│   │   ├── main.py
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── storage.py
│   │   └── logger.py
│   ├── Dockerfile
│   └── requirements.txt
│
├── ticket-service/
│   ├── app/
│   │   ├── main.py
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── storage.py
│   │   ├── session_client.py
│   │   └── logger.py
│   ├── Dockerfile
│   └── requirements.txt
│
├── payment-service/
│   ├── app/
│   │   ├── main.py
│   │   ├── schemas.py
│   │   ├── ticket_client.py
│   │   └── logger.py
│   ├── Dockerfile
│   └── requirements.txt
│
├── notification-service/
│   ├── app/
│   │   ├── main.py
│   │   ├── schemas.py
│   │   └── logger.py
│   ├── Dockerfile
│   └── requirements.txt
│
├── web-app/
│   ├── public/
│   │   └── index.html
│   ├── Dockerfile
│   ├── nginx.conf
│   └── .dockerignore
│
└── docker-compose.yml
```

### Добавление нового функционала

1. **В Session Service**: Добавьте методы в `main.py`
2. **В Ticket Service**: Обновите логику бронирования
3. **В Payment Service**: Интегрируйте реальный платёжный шлюз
4. **В Web App**: Добавьте новые секции в `public/index.html`

## 📝 Логирование

Каждый микросервис создаёт лог-файл в папке `logs/`:

```
logs/
├── session-service.log
├── ticket-service.log
├── payment-service.log
└── notification-service.log
```

Логи также выводятся в консоль (STDOUT).

## 🔒 Безопасность (Production)

В продакшене рекомендуется:

1. Использовать переменные окружения для конфигурации
2. Добавить аутентификацию (JWT)
3. Включить HTTPS
4. Добавить rate limiting
5. Валидировать все входные данные
6. Использовать реальную БД вместо in-memory storage

## 🤝 Troubleshooting

### Сервисы не запускаются
```bash
# Проверьте логи
docker-compose logs [service-name]

# Пересоберите контейнеры
docker-compose build --no-cache
```

### Ошибка подключения между сервисами
```bash
# Проверьте сетевые подключения
docker network inspect cinema-network

# Перезапустите docker-compose
docker-compose down && docker-compose up -d
```

### CORS ошибки
- Убедитесь, что все сервисы имеют CORSMiddleware
- Проверьте адреса в JavaScript консоли браузера

## 📄 Лицензия

MIT

## 👥 Автор

Создано для курсовой работы по микросервисной архитектуре.

---

**Статус**: ✅ Полностью функционально и готово к использованию на Debian 12 с Docker
